import styled from "styled-components";
import StyledIcon from "./StyledIcon";

const StyledFooterIcon = styled(StyledIcon)`
&&&{
    color: var(--red);
    margin: 0.5em;
}
`

export default StyledFooterIcon